README
In its current implementation
A popup button is added as a browser action button.

content.js is added as a content script. Its accompanying HTML is in test1.html


background1.js tries to inject code into user page programmatically. Its accompanying HTML user page is test.html
It also adds a Badge and an incrementing Badgetext on the popup button.

